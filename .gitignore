# Virtual environment folder - contains Python packages installed for this project
# Should not be committed as each developer should create their own virtual environment
.venv

# Environment variables file - typically contains sensitive data like API keys, 
# database passwords, secret keys that should NEVER be shared in Git
env.py

# Python cache directories - automatically generated by Python to speed up imports
# These are recreated automatically and differ across systems
__pycache__

# Compiled Python files - created when Python runs .py files
# *.py[cod] matches: .pyc (compiled), .pyo (optimized), .pyd (Windows DLL)
# These are auto-generated and can be recreated from source files
*.py[cod]

# SQLite database file - your local development database
# Contains actual data that may differ per developer, should not be version controlled
# Production databases are separate and managed differently
db.sqlite3